import { StagePlaceholder } from "@/components/shared/StagePlaceholder";

export default function RankingPage() {
  return <StagePlaceholder title="Ranking Geral" stage="ETAPA 6 (Cálculo do ranking)" />;
}
