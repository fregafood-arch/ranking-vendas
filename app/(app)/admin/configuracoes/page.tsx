import { StagePlaceholder } from "@/components/shared/StagePlaceholder";

export default function AdminSettingsPage() {
  return (
    <StagePlaceholder
      title="Administração — Configurações"
      stage="ETAPA 6 (Cálculo do ranking)"
    />
  );
}
